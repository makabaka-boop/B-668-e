FROM node:20-alpine AS build
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install || npm install --legacy-peer-deps
COPY . .
CMD ["sh", "-c", "npm run test || npm run test -- --reporter=default"]
